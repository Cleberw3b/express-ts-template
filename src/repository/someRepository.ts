//
//      Repository
//

interface SomeType { }
/**
 * Save
 * 
 * @param param
 */
export const save = async ( param: SomeType ) => {
    try {
        // TODO -> Implement
    } catch ( error ) {
        console.error( error )
    }
}

/**
 * Load
 * 
 * @param param - some
 */
export const load = async ( param: SomeType ) => {
    try {
        // TODO -> Implement
    } catch ( error ) {
        console.error( error )
        return null
    }
}
